<div class="container-fluid px-0">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="collapse navbar-collapse" id="navbarTogglerDemo03">
      <form class="form-inline my-2 my-lg-0">
        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      </form>
    </div>
    <div class="float-right">
      <% if user_signed_in? %>
        <div class="btn-group">
          <a href="#"><%=link_to 'Log Out', destroy_user_session_path, method: :delete, class: 'dropdown-item text-danger'%></a>
        </div>
      <% else %>
        <div class="btn-group">
          <a  href="#"><%= link_to "Sign In", new_user_session_path, class: "dropdown-item text-info" %></a>
          <a href="#"><%=link_to 'Sign Up', new_user_registration_path, class: 'dropdown-item text-info'%></a>
        </div>
      <% end %>
    </div>
  </nav>
</div>
